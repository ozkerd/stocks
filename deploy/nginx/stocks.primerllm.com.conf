server {
    listen 80;
    listen [::]:80;
    server_name stocks.primerllm.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name stocks.primerllm.com;

    # SSL certificates (Cloudflare Origin Certificate or Let's Encrypt)
    ssl_certificate /etc/ssl/certs/stocks.primerllm.com.pem;
    ssl_certificate_key /etc/ssl/private/stocks.primerllm.com.key;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 90;
    }

    location /static/ {
        alias /opt/stocks/static/;
        expires 7d;
        add_header Cache-Control "public, no-transform";
    }
}
