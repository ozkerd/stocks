# ControlResell — Architecture Graph

> Generated by `graphify` on 2026-09-22 11:07 UTC from 0 repos (0 Kotlin + 0 TS files, 0.0s). **Do not edit by hand** — regenerate with `./graphify build`.

`0` nodes, `0` edges. This file is the *cross-service surface only*. Everything else is queryable:

```bash
./graphify key platforms.jobs.response    # who publishes/consumes a routing key
./graphify contract JobPostsCreatePayload # a DTO's fields, producers, consumers
./graphify callers SendJobUseCase         # who calls this, transitively
./graphify impact RoutingKey              # blast radius of a change
./graphify deps controlresell-backend     # what a repo pins, and who pins it
```

## 0. Topology at a glance

Edges are derived from real publish/subscribe call sites; the label is the number of
distinct routing keys flowing that way.

```mermaid
flowchart LR
```

## 1. Services and what they speak


## 2. Build dependency graph (auto-derived pin table)

Each repo's catalogue entries **that a build file actually wires in**, resolved. A coordinate declared in `libs.versions.toml` but never referenced is omitted, as is one that arrives transitively through another module's `api()`. Versions drift per consumer by design — this table is generated, so it cannot go stale.


## 3. RabbitMQ routing keys

`0` keys. Most ride the shared topic exchange `controlresell`; `0` are published to a **headers** exchange instead (`controlresell-connector-consumer` for job dispatch to the slaves, and the per-account suspend exchange), where the routing key travels with the message but does not route it. A key with no publisher or no subscriber is worth a second look.

| Routing key | Publishers | Subscribers | Payload |
|---|---|---|---|

⚑ = `isMultiple`: fans out to a per-pod exclusive queue instead of the service queue.  
‡ = the queue binds this key but no `when (routingKey)` branch was found for it. That is a *hint to check*, not a defect: several services dispatch through a different shape. Confirm before acting on it.  
⇥ = routed by the **headers** exchange, not this topic exchange; it will never have a topic subscriber (job dispatch to the slaves works this way).

## 4. Job contract — which service handles which `JobPayload`

`JobPayload` has **0** variants, `JobResponse` **0**. The wire discriminator is the default kotlinx `"type"` field, valued by `@SerialName`.

## 6. Wire contracts

Counting only the published contract modules (`*-commons`, `*-api`) — the types another service actually compiles against:

- **0** `@Zodable` types () — generated into the npm packages, so the TypeScript app sees them.
- **0** `@Serializable`-only types — real cross-service payloads (RabbitMQ bodies) that never reach TypeScript.
- (0 further `@Serializable` types live outside the contract modules — marketplace API shapes, CDP bindings. Internal, not a ControlResell wire contract.)

| Module | DTOs | of which `@Zodable` |
|---|--:|--:|

## 10. Coupling hotspots

Highest in-degree nodes — changing one of these has the widest blast radius.

| Type | Kind | Repo | Referenced by |
|---|---|---|--:|

## 11. Queryable, but not printed here

These are large enough that dumping them would defeat the point of a summary.

| What | How to ask |
|---|---|
| 5 verified conventions and gotchas, with evidence | `graphify know <topic>` |

## 12. What this graph does and does not see

- **Static only.** Reflection, `ServiceLoader`, and runtime-constructed queue names are followed only where the pattern is recognised (the slave SPI is; a dynamically built routing key is not).
- **Test sources excluded** by default (`--include-tests` to include them).
- **Edges are never guessed.** A reference is only linked when it resolves to a declaration, and resolution is constrained by the build graph — so a repo can only reference repos whose artifacts it actually pins. That favours precision over recall: a missing edge is likelier than a wrong one.
- **Measured recall on the one thing that can be checked exactly:** `0.0%` of `send*` publish call sites in source became graph edges (0/0). `./graphify doctor` fails the build if that drops below 90%.
- **A stale local checkout produces a stale graph.** Repo versions and commit SHAs are recorded below so this is visible rather than silent.

Repos listed with how much of the graph they actually contribute — a low count means the repo is present but barely analysed (docs-only, or not Kotlin/TS).

| Repo | Version | HEAD | Nodes |
|---|---|---|--:|

